#include <iostream> // подключение библиотеки, для возможности вывода в консоль
using namespace std; // указание пространства имён, для кода ниже
void main()
{
	setlocale(LC_ALL, "Rus"); //делает расшифровку для русских символов
	int a = 1, b = 1, i,n; //a и b два самых первых числа в ряду чисел Фибоначчи
	cin >> n;   //Вводим количество чисел после 2 единицы из ряда Ф.
	i = a + b;
	for(int c =1; c<=n;c++)
	{
		cout << i << endl;
		a = b;  //Формула счёта чисел Ф.
		b = i;
		i = a + b; 
	}

}
